# Regenerable voice pack (build via tools/build_voice_pack.py)
web/voice/
# Build artifacts
build/
DerivedData/
*.xcarchive
*.ipa
.DS_Store

# Android
android/build/
android/app/build/
android/.gradle/
android/local.properties
**/.DS_Store

# Signing material and API credentials — never commit
android/keystore.properties
*.p12
*.jks
*.keystore
*.p8
*.mobileprovision

# Raw 24k wavs kept for cheap pitch re-renders (not shipped)
ios/voice_raw/
